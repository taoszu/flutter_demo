
import 'package:flutter/material.dart';

import '../title_base_page.dart';

class LifePage extends TitleBasePage {
  LifePage({Key key, title:String}): super(title:title);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
    );
  }
}